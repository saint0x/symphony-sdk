import { Symphony } from './symphony';

// Create the singleton instance
const symphony = new Symphony();

// Export only the symphony instance
export { symphony };

// Default export is also the symphony instance
export default symphony; 