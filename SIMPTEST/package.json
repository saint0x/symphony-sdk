{
  "name": "simptest",
  "version": "1.0.0",
  "description": "Symphony SDK Example Implementation",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "bun test",
    "prestart": "cd .. && npm run build && cd SIMPTEST"
  },
  "dependencies": {
    "symphonic": "file:../"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "bun-types": "latest",
    "@types/node": "^20.4.8"
  }
}